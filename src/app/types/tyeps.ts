export type flight={
    destination: string,
    price: string,
    
}
export type order={
    destination: string,
    name: string,
    quantity: number

 }
 export type worker ={
     code: string,
     jobId: string,
     salary: number
 }